namespace Biglab.IO.Networking
{
    public enum ConnectionState
    {
        // Connection has been established
        Connected,

        // Connection has been lost ( or never established )
        Disconnected
    }
}